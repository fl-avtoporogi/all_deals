# Приложение для редактирования кодов бонусов (v2.0)

Веб-приложение для встраивания в Битрикс24 для управления кодами бонусов.

## Статус

✅ **Полностью рабочее приложение** (протестировано в Битрикс24)

## Учетные данные приложения

**Client ID:** `local.692466e0264de0.30890025`
**Client Secret:** `eBfqBtkAZ8fcU2D91Lv76fktxUTGQenKM4TH6EYEyCUz5tz55p`

Эти данные прописаны в `settings.php` и используются для OAuth авторизации.

## Возможности

- 📊 **Просмотр всех кодов бонусов** в удобной таблице
- ✏️ **Редактирование значений бонусов** прямо в интерфейсе
- 📤 **Массовый импорт из CSV** файла
- 🔍 **Поиск по коду** бонуса
- 🔒 **Контроль доступа** через белый список пользователей
- 📝 **Логирование всех изменений**
- ⚡ **Автоматическая инвалидация кэша**

## Структура файлов

```
app_bonus_edit_new/
├── index.php              # Главный обработчик (точка входа)
├── install.php            # Обработчик установки OAuth
├── api.php                # Backend API для CRUD операций
├── config.php             # Конфигурация (БД, кэш, логи)
├── app.js                 # JavaScript логика интерфейса
├── styles.css             # Стили интерфейса
├── crest.php              # Библиотека CRest (OAuth)
├── crestcurrent.php       # Расширение CRest для текущей сессии
├── settings.php           # OAuth credentials
├── settings.json          # OAuth токены (создаётся при установке)
├── logs/                  # Папка для логов
│   └── bonus_changes.log  # Лог изменений
├── test_api.php           # Тестовый файл для диагностики
├── README.md              # Эта инструкция
└── MIGRATION_COMPLETE.md  # История миграции
```

## URL приложения

**Production:**
- Обработчик: `https://9dk.ru/webhooks/avtoporogi/all_deals/app_bonus_edit_new/index.php`
- Установка: `https://9dk.ru/webhooks/avtoporogi/all_deals/app_bonus_edit_new/install.php`

## Установка в Битрикс24

### Шаг 1: Создать приложение

1. Битрикс24 → Приложения → Разработчикам → **Добавить приложение**
2. Заполнить форму:
   - **Название:** Редактор бонусов
   - **Код:** bonus_editor
   - **Тип:** Локальное приложение

### Шаг 2: Настроить учетные данные

```
Client ID: local.692466e0264de0.30890025
Client Secret: eBfqBtkAZ8fcU2D91Lv76fktxUTGQenKM4TH6EYEyCUz5tz55p
```

### Шаг 3: Указать URL обработчиков

```
Путь вашего обработчика:
https://9dk.ru/webhooks/avtoporogi/all_deals/app_bonus_edit_new/index.php

Путь для первоначальной установки:
https://9dk.ru/webhooks/avtoporogi/all_deals/app_bonus_edit_new/install.php
```

### Шаг 4: Настроить интерфейс

- ✅ Встроить в левое меню
- Права: минимальные

### Шаг 5: Установить приложение

После сохранения приложение автоматически установится. Проверьте:
- Файл `settings.json` создан в папке приложения
- При открытии видна таблица с кодами бонусов

## Использование

### Редактирование значений бонусов

1. Откройте приложение в Битрикс24
2. Найдите нужный код в таблице
3. Измените значение в поле "Бонус"
4. Нажмите **"Сохранить все изменения"**
5. Дождитесь подтверждения

### Импорт из CSV файла

1. Подготовьте CSV файл в формате:
   ```csv
   Код бонуса;Бонус
   A1;35
   A2;40
   B5;50
   ```

2. Нажмите **"Выберите файл"** в секции "Импорт из CSV"
3. Выберите ваш CSV файл
4. Нажмите **"Импортировать"**
5. Проверьте результат

**Примечания:**
- Кодировка: UTF-8 или Windows-1251 (автоматически определяется)
- Разделитель: точка с запятой (`;`)
- Обновляются только существующие коды

### Поиск по коду

Введите часть кода в поле **"Поиск по коду..."** (например, "A" или "B10")

## Технические детали

### Подключение к БД

Приложение использует файл `db_connect.php`, который ищется в двух местах:
1. `../../db_connect.php` (2 уровня вверх)
2. `../../../db_connect.php` (3 уровня вверх)

Структура на сервере:
```
/webhooks/avtoporogi/
├── db_connect.php
└── all_deals/
    └── app_bonus_edit_new/
```

### Кэш

Файл кэша: `../bonus_codes_cache.json` (в папке `all_deals/`)

После каждого изменения кэш автоматически удаляется.

### Логирование

Все изменения записываются в `logs/bonus_changes.log`:

```
[2025-11-24 20:30:15] User: 366ab59d560d5e905f3ac1ec83ebc8d2 | Action: update | Changes: [{"code":"A1","bonus":40}]
```

### Контроль доступа

По умолчанию доступ разрешён всем. Для ограничения отредактируйте `config.php`:

```php
$whitelist_user_ids = [123, 456, 789]; // ID пользователей из Битрикс24
```

## API Endpoints

### GET /api.php?action=list

Получить список всех кодов бонусов.

**Параметры:**
- `member_id` (optional) - ID пользователя из Битрикс24

**Ответ:**
```json
{
  "success": true,
  "data": [
    {"code": "A1", "bonus": 35},
    {"code": "A2", "bonus": 35}
  ]
}
```

### POST /api.php?action=update

Обновить значения бонусов.

**Параметры:**
- `member_id` (optional) - ID пользователя

**Тело запроса:**
```json
{
  "codes": [
    {"code": "A1", "bonus": 40},
    {"code": "B5", "bonus": 55}
  ]
}
```

**Ответ:**
```json
{
  "success": true,
  "updated": 2,
  "errors": []
}
```

### POST /api.php?action=import_csv

Импортировать из CSV файла.

**Параметры:**
- `member_id` (optional) - ID пользователя
- `csv_file` (file) - CSV файл

**Ответ:**
```json
{
  "success": true,
  "updated": 5,
  "errors": ["Line 10: Code 'X99' does not exist"],
  "total_lines": 12
}
```

## Диагностика

### Тестовый файл

Для проверки подключения к БД откройте:
```
https://9dk.ru/webhooks/avtoporogi/all_deals/app_bonus_edit_new/test_api.php
```

Должен вернуть JSON с информацией о подключении.

### Типичные проблемы

**Проблема:** Спиннер не исчезает

**Решение:**
1. Проверьте консоль браузера (F12)
2. Убедитесь, что `settings.json` создан
3. Проверьте ответ от API: откройте в новой вкладке
   ```
   https://9dk.ru/webhooks/avtoporogi/all_deals/app_bonus_edit_new/api.php?action=list&member_id=test
   ```

**Проблема:** Ошибка "db_connect.php not found"

**Решение:**
1. Проверьте наличие файла на сервере
2. Откройте `test_api.php` для диагностики

**Проблема:** Данные не загружаются

**Решение:**
1. Проверьте таблицу `bonus_codes` в БД
2. Убедитесь, что пользователь БД имеет права SELECT

## История версий

### v2.0 (2025-11-24)
- ✅ Миграция из `app_bonus_edit` в `app_bonus_edit_new`
- ✅ Исправлен путь к `db_connect.php` (поиск в 2 местах)
- ✅ Полностью рабочая OAuth авторизация
- ✅ Протестировано в Битрикс24

### v1.0 (2025-11-23)
- Первая версия в `app_bonus_edit`
- Базовый функционал редактирования

## Техническая информация

- **PHP:** 7.4+
- **MySQL:** 5.7+
- **Браузер:** Chrome 51+, Firefox 54+, Safari 10+
- **SSL:** Обязателен (для работы в iframe Битрикс24)

## Безопасность

- ✅ Prepared statements (защита от SQL-инъекций)
- ✅ Валидация входных данных
- ✅ Контроль доступа через whitelist
- ✅ Транзакции БД
- ✅ Логирование всех изменений

## Поддержка

При возникновении проблем:
1. Проверьте консоль браузера (F12)
2. Откройте `test_api.php` для диагностики
3. Проверьте логи: `logs/bonus_changes.log`
4. Проверьте логи PHP на сервере

## Лицензия

Внутреннее использование компании Автопороги.

---

**Приложение готово к использованию!** 🚀
