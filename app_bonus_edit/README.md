# Приложение для редактирования кодов бонусов (v3.1)

Веб-приложение для встраивания в Битрикс24 для управления кодами бонусов с модульной архитектурой.

## Статус

✅ **Полностью рабочее приложение** (протестировано в Битрикс24)
✅ **Модульная архитектура** (рефакторинг v3.0)
✅ **Дизайн в стиле Bitrix24** (обновление v3.1)

## Учетные данные приложения

**Client ID:** `local.692466e0264de0.30890025`
**Client Secret:** `eBfqBtkAZ8fcU2D91Lv76fktxUTGQenKM4TH6EYEyCUz5tz55p`

Эти данные прописаны в `settings.php` и используются для OAuth авторизации.

## Возможности

- 📊 **Просмотр всех кодов бонусов** в компактной 3-колоночной таблице (с естественной сортировкой)
- ✏️ **Редактирование значений бонусов** прямо в интерфейсе
- 📤 **Массовый импорт из CSV** файла
- 🔍 **Поиск по коду** бонуса
- 🔒 **Контроль доступа** через белый список пользователей
- 📝 **Логирование всех изменений**
- ⚡ **Автоматическая инвалидация кэша**
- 🎨 **Минималистичный дизайн в стиле Bitrix24**

## Архитектура (v3.0)

Приложение использует **MVC-подобную архитектуру** с разделением на слои:

```
app_bonus_edit/
├── src/                           # Исходный код приложения
│   ├── autoload.php              # PSR-4 автозагрузчик
│   ├── Controllers/              # HTTP обработчики
│   │   └── BonusController.php  # Контроллер бонусов
│   ├── Services/                 # Бизнес-логика
│   │   ├── BonusService.php     # Сервис управления бонусами
│   │   └── CsvImportService.php # Сервис импорта CSV
│   ├── Repository/               # Доступ к данным
│   │   └── BonusRepository.php  # Репозиторий бонусов
│   ├── Core/                     # Ядро приложения
│   │   ├── Config.php           # Управление конфигурацией
│   │   ├── Database.php         # Подключение к БД
│   │   ├── Logger.php           # Логирование
│   │   └── Cache.php            # Управление кэшем
│   └── Utils/                    # Утилиты
│       ├── AccessControl.php    # Контроль доступа
│       └── Response.php         # HTTP ответы
├── public/                       # Публичные файлы
│   ├── js/
│   │   └── app.js               # JavaScript логика
│   └── css/
│       └── styles.css           # Стили интерфейса
├── index.php                     # Точка входа (UI)
├── api.php                       # REST API endpoint
├── install.php                   # Установка OAuth
├── crest.php                     # CRest SDK
├── crestcurrent.php              # CRest Current User
├── settings.php                  # OAuth credentials
├── settings.json                 # OAuth токены (создаётся при установке)
├── logs/                         # Логи приложения
│   └── bonus_changes.log
├── README.md                     # Эта инструкция
├── REFACTORING.md                # Описание рефакторинга
└── MIGRATION_COMPLETE.md         # История миграции
```

### Преимущества новой архитектуры

- ✅ **Разделение ответственности (SRP)** - каждый класс выполняет одну задачу
- ✅ **Легкость тестирования** - компоненты независимы
- ✅ **Масштабируемость** - легко добавлять новые функции
- ✅ **Поддерживаемость** - понятная структура кода
- ✅ **Повторное использование** - компоненты можно использовать в других проектах

Подробнее о рефакторинге: [REFACTORING.md](REFACTORING.md)

## URL приложения

**Production:**
- Обработчик: `https://9dk.ru/webhooks/avtoporogi/all_deals/app_bonus_edit/index.php`
- Установка: `https://9dk.ru/webhooks/avtoporogi/all_deals/app_bonus_edit/install.php`

## Установка в Битрикс24

### Шаг 1: Создать приложение

1. Битрикс24 → Приложения → Разработчикам → **Добавить приложение**
2. Заполнить форму:
   - **Название:** Редактор бонусов
   - **Код:** bonus_editor
   - **Тип:** Локальное приложение

### Шаг 2: Настроить учетные данные

```
Client ID: local.692466e0264de0.30890025
Client Secret: eBfqBtkAZ8fcU2D91Lv76fktxUTGQenKM4TH6EYEyCUz5tz55p
```

### Шаг 3: Указать URL обработчиков

```
Путь вашего обработчика:
https://9dk.ru/webhooks/avtoporogi/all_deals/app_bonus_edit/index.php

Путь для первоначальной установки:
https://9dk.ru/webhooks/avtoporogi/all_deals/app_bonus_edit/install.php
```

### Шаг 4: Настроить интерфейс

- ✅ Встроить в левое меню
- Права: минимальные

### Шаг 5: Установить приложение

После сохранения приложение автоматически установится. Проверьте:
- Файл `settings.json` создан в папке приложения
- При открытии видна таблица с кодами бонусов

## Использование

### Редактирование значений бонусов

1. Откройте приложение в Битрикс24
2. Найдите нужный код в таблице (коды отсортированы: A1, A2, ..., A10, A11)
3. Измените значение в поле "Бонус"
4. Нажмите **"Сохранить все изменения"**
5. Дождитесь подтверждения

### Импорт из CSV файла

1. Подготовьте CSV файл в формате:
   ```csv
   Код бонуса;Бонус
   A1;35
   A2;40
   B5;50
   ```

2. Нажмите **"Выберите файл"** в секции "Импорт из CSV"
3. Выберите ваш CSV файл
4. Нажмите **"Импортировать"**
5. Проверьте результат

**Примечания:**
- Кодировка: UTF-8 или Windows-1251 (автоматически определяется)
- Разделитель: точка с запятой (`;`)
- Обновляются только существующие коды

### Поиск по коду

Введите часть кода в поле **"Поиск по коду..."** (например, "A" или "B10")

## Технические детали

### Подключение к БД

Приложение использует файл `db_connect.php`, который ищется в двух местах:
1. `../../db_connect.php` (2 уровня вверх)
2. `../../../db_connect.php` (3 уровня вверх)

Структура на сервере:
```
/webhooks/avtoporogi/
├── db_connect.php
└── all_deals/
    └── app_bonus_edit/
```

### Кэш

Файл кэша: `../bonus_codes_cache.json` (в папке `all_deals/`)

После каждого изменения кэш автоматически удаляется.

### Логирование

Все изменения записываются в `logs/bonus_changes.log`:

```
[2025-11-24 20:30:15] User: 366ab59d560d5e905f3ac1ec83ebc8d2 | Action: update | Changes: [{"code":"A1","bonus":40}]
```

### Контроль доступа

По умолчанию доступ разрешён всем. Для ограничения используйте `AccessControl`:

```php
// src/Core/Config.php
$this->config = [
    'whitelist_user_ids' => [123, 456, 789], // ID пользователей из Битрикс24
    // ...
];
```

### Сортировка кодов

Коды бонусов сортируются с использованием **естественной сортировки** (natural sort):
- ✅ Правильно: A1, A2, A3, ..., A9, A10, A11, A12
- ❌ Неправильно: A1, A10, A11, A12, A2, A3

Реализовано в [BonusRepository::findAll()](src/Repository/BonusRepository.php) с помощью `strnatcmp()`.

## API Endpoints

### GET /api.php?action=list

Получить список всех кодов бонусов.

**Параметры:**
- `member_id` (optional) - ID пользователя из Битрикс24

**Ответ:**
```json
{
  "success": true,
  "data": [
    {"code": "A1", "bonus": 35},
    {"code": "A2", "bonus": 35}
  ]
}
```

### POST /api.php?action=update

Обновить значения бонусов.

**Параметры:**
- `member_id` (optional) - ID пользователя

**Тело запроса:**
```json
{
  "codes": [
    {"code": "A1", "bonus": 40},
    {"code": "B5", "bonus": 55}
  ]
}
```

**Ответ:**
```json
{
  "success": true,
  "updated": 2,
  "errors": []
}
```

### POST /api.php?action=import_csv

Импортировать из CSV файла.

**Параметры:**
- `member_id` (optional) - ID пользователя
- `csv_file` (file) - CSV файл

**Ответ:**
```json
{
  "success": true,
  "updated": 5,
  "errors": ["Line 10: Code 'X99' does not exist"],
  "total_lines": 12
}
```

## Диагностика

### Проверка работоспособности

1. **Проверка загрузки:**
   - Откройте приложение в Битрикс24
   - Должна загрузиться таблица с кодами

2. **Проверка API:**
   ```
   https://9dk.ru/webhooks/avtoporogi/all_deals/app_bonus_edit/api.php?action=list&member_id=test
   ```
   Должен вернуть JSON с кодами бонусов

3. **Проверка логов:**
   - Файл `logs/bonus_changes.log` должен содержать записи об изменениях

### Типичные проблемы

**Проблема:** Спиннер не исчезает

**Решение:**
1. Проверьте консоль браузера (F12)
2. Убедитесь, что `settings.json` создан
3. Проверьте ответ от API в новой вкладке

**Проблема:** Ошибка "db_connect.php not found"

**Решение:**
1. Проверьте наличие файла на сервере
2. Убедитесь, что путь правильный

**Проблема:** Данные не загружаются

**Решение:**
1. Проверьте таблицу `bonus_codes` в БД
2. Убедитесь, что пользователь БД имеет права SELECT
3. Проверьте консоль браузера на ошибки

## История версий

### v3.1 (2025-11-24)
- ✅ **Дизайн в стиле Bitrix24** - минималистичный, деловой, чистый
- ✅ Компактная 3-колоночная таблица (вместо одной длинной)
- ✅ Упрощенные категории (только буква "A" или "B")
- ✅ Новая цветовая палитра:
  - Фон: #F5F7FA (светло-серый Bitrix24)
  - Акцент: #2FC6F6 (голубой Bitrix24)
  - Успех: #55D0A4 (зеленый)
  - Границы: #E0E0E0 (тонкие, 1px)
  - Минимальные тени (flat design)

### v3.0 (2025-11-24)
- ✅ **Полная модуляризация архитектуры**
- ✅ Реализована MVC-подобная структура
- ✅ PSR-4 autoloader
- ✅ Разделение на Controller, Service, Repository, Core, Utils
- ✅ api.php сокращен с 294 до 32 строк
- ✅ Исправлена сортировка кодов (natural sort)
- ✅ Создана подробная документация (REFACTORING.md)

### v2.0 (2025-11-24)
- ✅ Миграция из `app_bonus_edit` в `app_bonus_edit_new`
- ✅ Исправлен путь к `db_connect.php` (поиск в 2 местах)
- ✅ Полностью рабочая OAuth авторизация
- ✅ Протестировано в Битрикс24

### v1.0 (2025-11-23)
- Первая версия в `app_bonus_edit`
- Базовый функционал редактирования

## Техническая информация

- **PHP:** 7.4+
- **MySQL:** 5.7+
- **Браузер:** Chrome 51+, Firefox 54+, Safari 10+
- **SSL:** Обязателен (для работы в iframe Битрикс24)
- **Архитектура:** MVC + Repository + Service
- **Autoloader:** PSR-4

## Безопасность

- ✅ Prepared statements (защита от SQL-инъекций)
- ✅ Валидация входных данных
- ✅ Контроль доступа через whitelist
- ✅ Транзакции БД
- ✅ Логирование всех изменений
- ✅ Изоляция слоев (separation of concerns)

## Разработка

### Добавление нового endpoint

1. Создать метод в [BonusController](src/Controllers/BonusController.php)
2. Добавить case в `handleRequest()`
3. При необходимости создать метод в [BonusService](src/Services/BonusService.php)

Пример:
```php
// src/Controllers/BonusController.php
public function export() {
    $userId = $this->checkAccess();
    $bonuses = $this->bonusService->getAllBonuses();
    // Экспорт логика
    Response::success(['file' => 'export.csv']);
}
```

### Структура классов

- **Controller** - обрабатывает HTTP запросы, валидирует параметры
- **Service** - содержит бизнес-логику, валидацию данных
- **Repository** - выполняет SQL запросы к БД
- **Core** - базовые компоненты (Config, Database, Logger, Cache)
- **Utils** - вспомогательные классы (AccessControl, Response)

Подробнее: [REFACTORING.md](REFACTORING.md)

## Поддержка

При возникновении проблем:
1. Проверьте консоль браузера (F12)
2. Проверьте логи: `logs/bonus_changes.log`
3. Проверьте логи PHP на сервере
4. Изучите [REFACTORING.md](REFACTORING.md) для понимания архитектуры

## Лицензия

Внутреннее использование компании Автопороги.

---

**Приложение готово к использованию!** 🚀

**Версия:** 3.1 (Bitrix24 Design + Modular Architecture)
**Последнее обновление:** 24 ноября 2025
