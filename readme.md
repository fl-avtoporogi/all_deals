# Система расчета бонусов (v4.0)

## 📋 Описание

Интегрированная система для обработки сделок Bitrix24 с автоматическим расчетом бонусов и оборотов по категориям товаров A и B.

**Основные компоненты:**
- ✅ Webhook обработчик (`index.php`) - автоматический расчет при создании/изменении сделки
- ✅ Веб-приложение `app_bonus_edit` - управление кодами бонусов через интерактивный UI
- ✅ Скрипты пересчета (`refresh_deals_NEW.php`, `fast_update_deals.php`) - массовая переработка сделок

## ⚙️ Требования

- PHP 7.4+
- MySQL 5.7+
- Bitrix24 с доступом к API
- Файл конфигурации: `../db_connect.php` (один уровень выше папки `/dev`)

## 🚀 Быстрый старт

### 1️⃣ Инициализация базы данных

Убедитесь, что в БД существует таблица с правильной структурой:

```sql
CREATE TABLE bonus_codes (
    code VARCHAR(10) PRIMARY KEY,
    bonus_amount DECIMAL(10,2) NOT NULL,
    Name VARCHAR(100),
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP
);
```

### 2️⃣ Добавление кодов бонусов

Используйте веб-приложение для управления кодами:

**Установка приложения в Bitrix24:**
```
https://your-domain.com/webhooks/avtoporogi/all_deals/app_bonus_edit/install.php
```

**Открытие приложения:**
```
https://your-domain.com/webhooks/avtoporogi/all_deals/app_bonus_edit/index.php
```

**Функции приложения:**
- Просмотр всех кодов бонусов в таблице (разбито на 3 колонки для удобства)
- Редактирование бонусов с автосохранением
- Импорт из CSV файла (формат: `Код;Наименование;Бонус`)
- Поиск по коду и наименованию
- Отображение наименования товара/компонента для каждого кода
- Логирование всех изменений

### 3️⃣ Настройка Bitrix24

1. **Создайте бизнес-процесс** в Bitrix24 для вызова webhook при изменении сделки:
```
https://your-domain.com/webhooks/avtoporogi/all_deals/index.php?deal_id={{ID}}
```

2. **Убедитесь**, что товары в сделках заполнены полем "Код бонуса" (свойство product property221)

## 📊 Как это работает

### Алгоритм расчета в webhook

1. Получает ID сделки из параметра
2. Загружает товары сделки из Bitrix24 API
3. Для каждого товара:
   - Извлекает код бонуса из поля property221
   - Нормализует код (Cyrillic А,В → Latin A,B)
   - Определяет категорию (A или B по первой букве)
   - Вычисляет оборот и бонус
4. Сохраняет результаты в таблицу `all_deals`:
   - `turnover_category_a` - оборот по категории A
   - `turnover_category_b` - оборот по категории B
   - `bonus_category_a` - бонус по категории A
   - `bonus_category_b` - бонус по категории B
   - `quantity` - общее количество товаров

### Пример расчета

**Входные данные:**
| Товар | Кол-во | Цена | Код | Описание |
|-------|--------|------|-----|---------|
| Товар 1 | 2 | 1000₽ | A1 | Порог |
| Товар 2 | 1 | 500₽ | B5 | Усилитель 200 |
| Товар 3 | 3 | 200₽ | - | Без кода |

**Результат:**
- Оборот A: 2 × 1000 = 2000₽
- Оборот B: 1 × 500 = 500₽
- Бонус A: 2 × 35 = 70₽ (код A1 = 35₽/шт)
- Бонус B: 1 × 50 = 50₽ (код B5 = 50₽/шт)
- Товар без кода пропущен

## 📁 Структура проекта

```
/dev/
├── index.php                    # Основной webhook обработчик
├── refresh_deals_NEW.php        # Пересчет сделок (рекомендуется)
├── fast_update_deals.php        # Альтернатива с параллельной обработкой
├── app_bonus_edit/              # Веб-приложение управления бонусами
│   ├── index.php               # Интерфейс приложения
│   ├── api.php                 # API endpoints
│   ├── src/
│   │   ├── Repository/         # Работа с БД
│   │   ├── Services/           # Бизнес-логика
│   │   ├── Controllers/        # HTTP обработчики
│   │   ├── Core/               # Ядро (Database, Config, Logger)
│   │   └── Utils/              # Утилиты
│   ├── public/
│   │   ├── js/app.js          # Frontend логика
│   │   └── css/styles.css     # Стили Bitrix24
│   └── logs/                   # Логи изменений
└── README.md                    # Этот файл
```

## 🔄 Обновление кодов бонусов

**Способ 1: Через веб-приложение (рекомендуется)**
1. Откройте приложение `app_bonus_edit`
2. Используйте кнопку "Импорт из CSV"
3. Все изменения сохраняются автоматически

**Способ 2: Прямое редактирование**
1. Отредактируйте значения в таблице приложения
2. Изменения сохраняются с автосохранением

## 🔄 Массовая переработка сделок

Если нужно пересчитать все сделки (например, после обновления кодов бонусов):

### Новый способ (рекомендуется):
```bash
php refresh_deals_NEW.php              # Обработать все сделки
php refresh_deals_NEW.php limit 100    # Тест на 100 сделках
php refresh_deals_NEW.php reset        # Сброс прогресса
```

**Преимущества:**
- Автоматический прогресс
- Можно прерывать и продолжать
- Параллельная обработка
- Детальные логи

### Альтернативный способ (параллельная обработка):
```bash
php fast_update_deals.php
```

**Преимущества:**
- В 3-25x быстрее чем старый способ
- Параллельная HTTP обработка

## 💾 Структура БД

### Таблица `bonus_codes`

| Поле | Тип | Описание |
|------|-----|---------|
| `code` | VARCHAR(10) PRIMARY KEY | Код бонуса (A1, B5 и т.д.) |
| `bonus_amount` | DECIMAL(10,2) | Размер бонуса в рублях |
| `Name` | VARCHAR(100) | Наименование товара/компонента |
| `created_at` | TIMESTAMP | Дата создания записи |
| `updated_at` | TIMESTAMP | Дата последнего обновления |

### Таблица `all_deals` (обновляемые поля)

| Поле | Описание |
|------|---------|
| `quantity` | Общее количество товаров |
| `turnover_category_a` | Оборот по категории A (руб) |
| `turnover_category_b` | Оборот по категории B (руб) |
| `bonus_category_a` | Бонус по категории A (руб) |
| `bonus_category_b` | Бонус по категории B (руб) |

## 🔒 Безопасность и валидация

- **Нормализация кодов:** Cyrillic А,В → Latin A,B (защита от ошибок ввода)
- **Пустые коды:** Товары без кода пропускаются автоматически
- **Prepared statements:** Все SQL запросы используют подготовленные выражения (защита от SQL injection)
- **Логирование:** Все действия логируются в `app_bonus_edit/logs/bonus_changes.log`

## 📝 Логирование

**Главный лог приложения:** `app_bonus_edit/logs/bonus_changes.log`

Содержит:
- Дату/время изменения
- Пользователя (member_id)
- Измененный код
- Старое и новое значение
- Результат операции

## 🧪 Тестирование

**Тестировать webhook:**
```bash
curl "https://your-domain.com/webhooks/avtoporogi/all_deals/index.php?deal_id=DEAL_ID"
```

**Проверить работу приложения:**
1. Откройте `app_bonus_edit`
2. Проверьте загрузку данных
3. Отредактируйте значение
4. Убедитесь в сохранении

## 🐛 Отладка

**Если не работает:**

1. **Webhook не срабатывает**
   - Проверьте настройку бизнес-процесса в Bitrix24
   - Убедитесь в доступности URL

2. **Товары не загружаются**
   - Проверьте права API в Bitrix24
   - Убедитесь в наличии товаров в сделке

3. **Неверные расчеты**
   - Проверьте таблицу `bonus_codes`
   - Убедитесь в заполнении поля property221

4. **Приложение не открывается**
   - Проверьте доступ к файлу `../db_connect.php`
   - Убедитесь в наличии таблицы `bonus_codes` в БД

## 📚 Документация

Подробная информация в файлах:
- `app_bonus_edit/README.md` - документация приложения
- `CLAUDE.md` - инструкции для Claude Code
- `_docs/` - дополнительные материалы

## 🔄 История версий

- **v4.0** - Добавлено поле "Наименование" в БД и приложение
- **v3.1** - Переработано приложение в стиле Bitrix24
- **v3.0** - MVC архитектура для приложения
- **v2.0** - Полнофункциональное приложение управления кодами
- **v1.0** - Базовый webhook с импортом из CSV
